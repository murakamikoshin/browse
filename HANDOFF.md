# 引き継ぎ

新しいセッションを始める人へ。**まず `README.md` → `SPEC.md` の順に読んでください。**
ここには、その二つに書ききれない「進め方」だけを置きます。

## いま何ができているか

遊べる状態です。`game.html` を `file://` で開けば動きます（ビルド不要、外部通信なし）。

- 本文：第一章〜第七章、四つの封、最終行38本、ED名85個、夜の行動173本、
  手を動かすところ22箇所、章の引き6箇所、帳場さんの帳8節、引っかかり59本、
  額の読み（形10／端／格6）、スタッフロール —— **全部書き終わっている**
- 仕組み：支払い38段 → 帯5つ／残高 → 封4枚 → **エンディング85通り**、全部通る
- 画面：題の画面・説明3枚・名札・ふりがな・語注・音（合成）・章の札・記録・
  帳（結末の収集）・中断と再開・携帯対応
- 書体：クラフト明朝を作品が使う字ぶんだけ焼き込み済み。**外へは何も取りに行かない**
  （Google の書体を読みに行っていたのをやめた。`verify.mjs` が `https://` を字面で止める）
- 検査：`tools/verify.mjs` と `tools/playtest.mjs` が不備0件

**足りないのは絵と決済と法務だけ**です（`README.md` の「残っているもの」）。

## 画面の並び

    題　　　　包む／帳を見る／遊び方／設定
     ↓
    説明１　包む額のこと
    説明２　この夜のこと
    説明３　読むときのこと　　（どこからでも「戻る」）
     ↓
    支払い　額を決める　→　確認　→　本編
     ↓
    本編　　通夜（地図）→ 夜明け前 → 翌朝　出棺 → 結末 → 帳

**どの画面も一枚に収める。頁は動かさない**（`html,body{overflow:hidden}`）。
中で送るのは本文の窓だけ。被せ（帳・記録・設定）だけは中で動いてよい。
説明を足したくなったら、頁を伸ばさずに `INTRO` に一枚足すこと。

## 作業の型

原稿は `scenario/*.md` が正典で、`game.html` はそこから**生成**されます。
`game.html` の本文を直接いじらないでください。次の一往復で反映します。

```
scenario/step9_choba.md を直す  →  python3 tools/build_choba.py  →  game.html に入る
```

| 直したもの | 叩く道具 |
|---|---|
| 第1〜6章の本文 | `tools/build_ch.py`（`--check` で原稿との食い違いだけ出す） |
| 第7章・封・最終行 | `tools/build_ch7.py` |
| ED名 | `tools/build_ed.py`（`--slots` で未執筆の組を出す） |
| 夜の行動 | `tools/build_talks.py` |
| 夜の半ばの一場面（圧） | `tools/build_press.py` |
| 美波が起きてくる場面 | `tools/build_mina.py` |
| 手を動かすところ・章の引き | `tools/build_acts.py` |
| 話者 | `tools/build_speakers.py` |
| ふりがな | `tools/build_ruby.py`（`--report` で読みの割れ） |
| 帳場さんの帳 | `tools/build_choba.py` |
| 額の読み | `tools/build_yomi.py` |
| 語注 | `tools/build_gloss.py` |
| 音の付く行 | `tools/build_marks.py` |
| 題のロゴ | `tools/build_logo.py` |
| 書体 | `tools/build_font.py`（`--check` で不足字） |
| 手がかり（帳のヒント） | `tools/build_hints.py` |
| 引っかかり | `tools/build_snags.py` |
| スタッフロール | `tools/build_credits.py` |
| 音声ファイルを置いたとき | `tools/build_audio.py` |

周回の測り方：

```
NODE_PATH=/opt/node22/lib/node_modules node tools/sweep.mjs 40 777 > /tmp/s.json
python3 tools/sweep_report.py /tmp/s.json
```

**何周目で新しい行が尽きるか**を測る道具。行き止まり・落ちの発見にも使える。

いまの実測（四十周・種777）——

    覚えた行 1,960（四十周で触れた分。行動173本のうち77本まで）
    1周目   489行が初めて
    2〜3周   117行／周
    4〜10周   85〜90行／周
    11〜25周  26〜34行／周
    26〜50周  10行／周
    **30周目で95%読了**

夜の行動を105→173本に増やし、その173本すべてに帯の書き換えを入れ、
夜の半ばに凪さんの場面を足した結果。増やす前は 1,300行・49周目で95%で、
2〜10周が50行前後だった。いまは 4〜10周でも85〜90行が初めて読む行になる。
（「何周目で95%」は四十周で触れた分に対する割合なので、本文を増やすと
かえって早く見えることがある。**周あたりの初めて読む行のほうを見ること。**）
**何周でも新しくしたければ本文を増やすしかない**のは変わらない。
同じ種で走らせれば前後を比べられるので、増やしたら必ず測ること。

読み込み専用の検査（叩くと報告だけ出る）：

| 見るもの | 道具 |
|---|---|
| 指せる語句の値と重なり | `tools/check_asks.py` |
| 夜の順番（先出しの参照・未読の章のものを思い出していないか） | `tools/check_order.py` |
| ふりがなの読み違い・数え方 | `tools/check_ruby.py` |
| 本文が言う数と実装の数の食い違い | `tools/check_facts.py` |
| 括弧・重なり・句点・同じ一行の二度書き・長すぎる行 | `tools/check_prose.py` |

第1〜6章も `scenario/step1_ch{1..6}_band1.md` と `step2_ch{1..6}_bands.md` が正典で、
`tools/build_ch.py` が `game.html` の `var CH` を作ります。**`game.html` の本文を
直接直すと、次に誰かが道具を叩いた瞬間に消えます**（実際に一度そうなりかけました。
章4の「どの順で回っても通る言い方」への直しが `game.html` にしか入っていなかった）。
`python3 tools/build_ch.py --check` が食い違いだけを出すので、迷ったらこれを叩く。

直したら**必ず二つ通す**：

```
NODE_PATH=/opt/node22/lib/node_modules node tools/verify.mjs     # データの整合
NODE_PATH=/opt/node22/lib/node_modules node tools/playtest.mjs   # 遊ぶ側から（10分ほど）
```

`playtest.mjs` は実際に3回、本物の行き止まりを見つけています。飛ばさないでください。
急ぐときは `--quick`（額を三つだけ）。

## 触るときに壊しやすいところ

- **`game.html` の差し込み口はコメントで挟んである**（`/* ○○ ここから */ … /* ここまで */`）。
  道具はこの札を探して入れ替えます。札を消すと道具が落ちます。
- **CSS の規則を複数行まとめて置換しない。** 過去に何度も、規則の順番が変わっていて
  静かに失敗しました。一つずつ、置換後に `grep` で確認すること。
- **ふりがなは原稿に埋め込まない。** 辞書で当てています。埋め込むと「指せる語句」が壊れます。
- **音は全部その場で合成**しています。ファイルは一つも積んでいません。

## 取り違えやすい細かいこと

- **汐里は洋装の喪服。着物なのは凪さん。** 第1章 L015 に「靴を脱ぐ前に」とある。
  夜の行動に「着物の袖」と書いてしまい、あとで全部直した
- **父の葬式のとき、汐里は十七。** 東京へ出たのは三年前なので、
  父のときの帰り道に「東京へ帰る電車」は無い
- **石段は十段。表札は「巽」。線香は十六本。帳場さんの机は四十年。**
  数を書いたら `python3 tools/check_facts.py` を叩く
- **買える語句（切符・小銭・封筒…）を、その章より前に出さない。**
  `tools/check_order.py` が拾う

## 絶対に破ってはいけない線

作品の中心がここにあります。実装で迷ったらこれで判定してください。

1. **帯 = 主人公の自己認識。封 = 主人公が知らない他人の秘密。** 混ぜない
2. **売るのは事実だけ。解釈は売らない**
3. **低い帯は虫食いではなく、それ自体で筋の通った誤読**。伏線とは、誤読していた証拠
4. **最終行は開封数も購入の有無も知らない。** 同じ支払額なら全員が同じ一行
5. **¥0 には封の値段も不足額も見せない。** 襖の前にも立たせない
6. **どの額で何が変わるかを、事前にも事後にも表にして見せない。**
   「書き換わった行」は終わったあとに**位置だけ**光り、中身は出さない。
   帳も、どの額で何番のEDが出るかは書かない
7. **数字は隠さない。**「八十五」は見せる。隠すのは対応関係だけ
8. **語注は辞書の意味だけ。** その語がこの話で何を指すかを書いた時点で、封と同じものになる。
   人の名前と重なる語に注意（凪）。呼びかけが続くときは出さないようにしてある
9. **夜の五つの場所に章番号を付けない。** 歩いた順で番号が前後すると辻褄が合わない
9b. **どの順で回っても話が通ること。** 章3（港）・章4（机）・章5（石段）は互いを前提に
   できない。仏間の手も、そのどれも前提にできない。「思い出した」「さっき」と書く前に、
   その場面がその順で必ず読まれているかを確かめる。`tools/check_order.py` が機械で拾う。
   例外は章1（必ず最初）と章2（仏間を読まないとどこも開かない）だけ。
   **机は必ず仏間のあと**（章4は仏間の引き出しを指している）。朝へ通す道も同じ順を守る（`toDesk`）
10. **金を包んだ人は、帳場の机を通らずに朝へ出さない。** 買えることを知らないまま
    終わるのは事故。断る自由は残す（`lastStop` / `askDawn`）
11. **帯の段は、夜の中でも上がる。** 夜の行動173本すべてが帯で書き換わる
    （帯2 173行／帯3 161行／帯4 156行／帯5 180行）。額を一段上げると、
    173本のうち 173／158／156／170 本が書き換わる。ここが薄いと、
    章の本文しか変わらない額の段が「払っても何も起きない段」になる。
    **行動を足すときは、帯3と帯5を最低一行ずつ添える**こと。数は
    `python3 -c "..."` ではなく `tools/build_talks.py` の出力と
    `scenario/step6_talks.md` の帯見出しで数える。
    書き換えていいのは**汐里の地の文だけ**。相手の台詞は全帯で同じ（線1に同じ）

**夜のほうから動くところが三つある。**こちらから訊く以外に何も起きないと、
中盤が平らになる。線香の残りで、選ばずに焚かれる ——

    線香13本以下・仏間　　美波が起きてくる（`tools/build_mina.py`）
    　　　　　　　　　　　仏間へ戻らないまま10本を切ると、一度だけ音で呼ぶ
    線香 9本以下・どこでも　凪さんが探しに来る（`tools/build_nagi.py`）※港へは来ない
    線香 4本以下・どこでも　帳場さんが時間を言いに来る（`tools/build_press.py`）※港は隣のおじさん

凪さんの場面の本文は、行に出どころ（`凪` ＋ `L###`）を持たせてある。
`scenario/step10_snags.md` に `引 好きに　凪　L011` と書いた一本は、
**その場面を見た人だけ**が、あとの並べ直しで拾う。

順番を入れ替えるときは、`build_nagi.py` が「帳場さんの圧より前か」を機械で見ている。
**線香を一本も使わない遊び方では、三つとも焚かれない**（それでいい。夜を使わなかった人だ）。
通し試験は歩き方に頼らず、`__dev.beat(場所, 線香)` から直に焚いて見ている。

**封は、あなたが夜に何を見たかを知っている。**原稿に `〔見た キー〕` `〔見ていない キー〕`
と書くと、その引っかかりを夜に拾った人にだけ（拾わなかった人にだけ）出る行になる。
キーは `|` でいくつでも並べられ、「どれか一つでも」で判定する（`tools/build_ch7.py`）。
封三の締めがこれで変わる ——

    〔見た〕　　　あなたは、今夜、それを見ている。見て、いいことだと思った。
    〔見ていない〕あなたは、今夜、そのどれも見ていない。見ていなくても、書いてあることは変わらない。

**無料の夜が、課金の朝に効く**唯一の仕掛けなので、ここを薄くしないこと。

新しい層（台詞・封・帳場さんの帳など）を足すときは、
`tools/build_choba.py` の `NG` 表のような**機械で弾く語の一覧**を必ず付けてください。
人間の目視では漏れます。

## 直近のやりとりで決まったこと

- **現実の1円 = この町の100円。** 金九千円まで無料、金百万円で¥10,000（`README.md` の表）
- **章は一度読んだら流し直さない。** 再入場は「もう一度読む」から
- **線香16本に対して行動は173本。** 全部はできない、が設計
- **「何が起きたか」は買った額が決め、「何に気づいたか」は夜の過ごし方が決め、
  「町にどう聞こえたか」は読まれた額の形が決める。** 三つの軸は交わらない
- **額の読み**は帯でも封でもない三本目の軸。数の形（頭の数字・切りのよさ・
  町の額との並び）からしか決まらない。**帯で書き分けない**（聞こえたことは
  誰にも同じように聞こえる）。隣り合う段が同じ朝にならないための仕組みなので、
  ここを薄くすると「一段動かす意味」が消える
- **「どうする」は畳んだ札をそのまま戻すだけ。手を作り直さない。**
  作り直すと、手を動かすところや立ち止まりの手が、その場所の手に化けて、
  積んである行が捨てられる。章が読み終わらないまま出ると、次にそこへ入ったときに
  最初から流れ直して、一度したやり取りがもう一度起きる
- **手が出ているあいだ（畳んであるものも含む）は、窓を押しても送らない。**
  送ると、手を動かすところの一手が飛ぶ
- **場面の途中で線香が尽きても、その場面は読み終えてから朝にする**（`burnMid`）。
  途中で切ると章が読み終わらず、同じやり取りが二度起きる
- **帳場さんの帳の段は 5 / 15 / 30 / 50 / 85**
- **線香16本に対して行動百七十三本。** 一周で触れられるのは十分の一。
  **半分は「夜が更けてから」出る**（原稿の `[線香] N`／`late`）。七十本を一度に
  並べると頁が動くので、線香が減るにつれて一本ずつ増える。
  さらに **一度に並べるのは八本まで**（`HEREMAX`）。使えば次が出る。
  **原稿の順に八本だけ出すと後ろは一生出てこない**ので、夜の初めから在るものを四本、
  更けてから出たものを四本、混ぜて並べる（更けたほうは、いま開いたばかりのものを先に）。
  「ここでできることは、まだあります」は、その場所で一度だけ言う
  段ごとの薄さを補うために、額の読みは夜と朝の両方で効かせる
- **二周目のために、早送りは未読で止まる**（`SKIPNEW`／設定で切れる）。
  これが無いと、二周目は一万字を目で追いながら書き換わった行を探すことになる。
  昔いちど外した仕掛けだが、外した理由（押しても効かないように見える）は
  **止まった理由を必ず言う**ことで消してある
- **残りは数だけ見せる。** 題の画面・結末の札・帳に、見た朝／見た夜の手／
  聞いた数の形／町の並び。**何があるかは書かない**（SPEC 6）
- **同じ帯の中では、本文は一字も変わらない。** 帯2〜4 はそれぞれ九段あるが、
  九段が共有する本文は同じ。段ごとに違うのは **額の読み（形・端・格）と最終行だけ**。
  ここを薄くすると「一段動かす意味」が本当に消えるので、
  **額の読みは夜と朝の両方で効かせる**（`yomiUke` 第1章L042／`yomiMachi` 第4章L069）
- **夜に二度だけ、こちらが選ばずに何かが起きる。** ここが無いと中盤が平らになる。
  どちらも線香を使わない（訊いたのではない）。**場面の途中では焚かない。**
  1. **美波が起きてくる**（`MINA` / `minaDue`）。仏間で線香を三本使ったあと、一度だけ。
     **この夜で唯一、汐里が人に何かを渡す場面。**手は三つで、**どれも正しくない**。
     選んだ手は結末を動かさない（三つの軸は交わらない）。取り消せないことだけが残る
  2. **線香が四本になったら、帳場さんが机を離れて来る**（`PRESS` / `pressDue`）。
  こちらから訊く以外に何も起きないと、中盤が平らになる。**場面の途中では焚かない。**
  帳場さんは港まで来ないので、港だけは隣のおじさんが時間だけ言う（単独）
- **指せる語句は、教わったあと最初の一つだけ一度光る**（`.ask.hint`）。
  持続する印は付けない。SPEC 3.5 A の改訂として記録してある

## 書体と音の素材はリポジトリに無い

`.gitignore` で `fonts/*.otf` と `audio/*.mp3` などを弾いています。
クラフト明朝の規約が「フォントファイルのみの再配布」を禁じていて、
このリポジトリは公開だからです。**作品が使う862字は `game.html` に焼き込み済み**なので、
遊ぶだけなら何も要りません。焼き直すときだけ配布元から取ってきてください
（`fonts/README.md`）。

## 置き場所

- ブランチ：`claude/browser-game-monetized-story-ifhrfb`
- 試遊用に公開しているもの（Artifact）：`game.html` をそのまま上げているだけ

## 次に判断が要ること（作者に訊くこと）

作業者が勝手に決めてはいけないものです。

1. **決済を本当に通すか。→ `docs/RELEASE.md` に三つの道（甲・乙・丙）を並べた。**
   いま `game.html` は一枚の静的ファイルで、封の本文も帯5の書き換えも中に入っている。
   **このままでは売れない**（開発者ツールで全部読める）。決めるまでは丙（無料）のまま
2. **絵18枚。** `tools/verify.mjs` が枚数と名前を出す。仮の絵は `phUrl()` が
   その場で描いている（構図と光の位置だけ。`ASSETS.md`）
3. **`fonts/craftmincho.otf` が git の履歴に残っている**（`5ddb856`）。
   いまは `.gitignore` で弾いているが、履歴からは消えていない。
   公開リポジトリなので、履歴の書き換えをするかどうか

## 通しで遊んで見つけて、直したところ

- 玄関に手が五つ並んで、そこで夜の半分が終わっていた。二つ（魚屋の奥さん・
  朝の段取り）を仏間のあとに回した（原稿の `[あと] 2`）。玄関は三つで始まる
- 夜の形を最初に数で言うようにした（行けるところ五つ、手二十七、線香十四本）。
  線香が半分になって、まだ二箇所しか見ていない人には一度だけ声をかける
- 並べ直しの一段目を 5 個から **2 個**に下げた。何のために聞き回っているのかが、
  夜の早いうちに分かる
- 包まなかった人も、帳場の机を必ず通るようにした（`lastStop`）。買えないので
  値は出ないが、木箱の場面を見ずに終わる道は無くした
- **開幕で「夜を終える」を選ぶ人**が、机への道を一度も見せられずに朝になっていた。
  `askDawn` が机を勧めるのを金一万円（実額¥100）からに絞っていたためで、
  それ未満でも包んだ額から封は開ける。額で分けるのをやめた（SPEC 10）
- **朝へ通す机で買うと画面が止まっていた。** 買ったあとの締めが
  `if(burn()){ … return; }` で、線香がもう0のときは burn() が何もせずに true を返す。
  そのまま手を離すので、積んだ行も手も札も出ないまま止まる。いちばん買われる場所だった。
  `burnMid()` に変え、`afterOffer` が戻る先の無いときに朝待ちを拾うようにした
- 検査の「買った」印が、札の押しボタンだけを見ていた。買う道は二つ（札の「はい」と
  選択肢の「訊く」）あるので、**帳に載ったか**で見るようにした。
  ゲームは買えているのに検査だけが落ちていた
- 通し試験の「机を通ったか」は、**夜のあいだだけ**見るようにした。夜明け前も
  場所の札は `cha` のままなので、それまでは誰でも通ったことになっていて、
  この検査は何も見ていなかった。「仏間を読まずに机へ入った」も同じ理由で
  常に鳴っていた（つまり、鳴りっぱなしと鳴らずの二つが打ち消し合っていた）
